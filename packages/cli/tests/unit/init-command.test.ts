// packages/cli/tests/unit/init-command.test.ts
// Moved to tests/unit/onboarding/init.test.ts — see T8 of S2 onboarding wizard.
// This file is kept as a stub so the test runner doesn't break imports.
import { describe, it, expect } from "vitest";

describe("init-command (legacy)", () => {
  it("was replaced by the onboarding wizard — see tests/unit/onboarding/init.test.ts", () => {
    expect(true).toBe(true);
  });
});

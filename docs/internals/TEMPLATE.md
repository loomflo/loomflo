# {Feature Name}

## What
One-line summary of what this feature does.

## Why
Why this exists — the problem it solves that standard approaches don't.

## How
Key mechanics, data flow, or algorithm. Code references use `path:line` format.

## Files
- `path/to/main.ts` — primary implementation
- `path/to/support.ts` — supporting logic

## Gotchas
Non-obvious behaviors, edge cases, or known limitations.
